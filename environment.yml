name: fao_data
channels:
  - conda-forge
  - defaults
dependencies:
  - python >= 3.11.2
  - country_converter >= 1.0.0
  - numpy >= 1.24.2, < 2
  - pandas >= 2.0.0, < 2.4
  - python-dateutil >= 2.8.2
  - pyyaml >= 6.0
  - ray-all >= 2.3.1
  - requests >= 2.28.2
  - scikit-learn >= 1.2.2
  - scipy >= 1.10.1
  - openpyxl >= 3.1.5
  - XlsxWriter >= 3.2.0
  # testing and developing
  - pdbpp
  - ipython
  - jupyterlab >= 3.*

